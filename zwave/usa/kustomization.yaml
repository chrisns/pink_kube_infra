nameSuffix: -usa
bases:
 - ../base

secretGenerator:
- name: zwave
  behavior: merge
  env: env.txt
  type: Opaque

patches:
 - pv.yaml
 - pvc.yaml
 - deployment.yaml

generatorOptions:
  disableNameSuffixHash: true