kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: zwave
spec:
  template:
    spec:
      containers:
      - name: zwave
        env:
          - name: BUCKET_KEY
            value: pink_usa
      nodeSelector:
        kubernetes.io/hostname: stick5
